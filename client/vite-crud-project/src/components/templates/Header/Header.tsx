import AddButton from '../../molecules/AddButton';
import { StyledAddButton } from './styles';

const Header = () => {
  return (
    <StyledAddButton>
      <AddButton />
    </StyledAddButton>
  );
};

export default Header;
